import styled from 'styled-components'

export const CurrencyWrapper = styled.span`
  .currency-prefix {
    font-size: 2rem;
    line-height: 2rem;
    position: relative;
    bottom: 0.25rem;
    margin-right: .5rem;
  }

`
